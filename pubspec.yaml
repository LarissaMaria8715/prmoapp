name: equilibreapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  sqflite:
  path:
  intl: ^0.18.0
  fl_chart: ^0.65.0
  shared_preferences:
  dio:
  url_launcher: ^6.3.0
  http: ^1.1.0
  xml2json: ^6.0.2
  google_maps_flutter:
  geocoding: ^2.1.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/equilibre.jpg